% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter-scan-2d.R
\name{parameter_scan_2d}
\alias{parameter_scan_2d}
\title{Scan Two Operator Parameters}
\usage{
parameter_scan_2d(
  operator,
  parameter1,
  values1,
  parameter2,
  values2,
  f,
  grid,
  n = NULL
)
}
\arguments{
\item{operator}{An object of class \code{"approx_operator"}.}

\item{parameter1}{Character string giving the name of the first
parameter to be varied.}

\item{values1}{Numeric vector containing the values of the first
parameter.}

\item{parameter2}{Character string giving the name of the second
parameter to be varied.}

\item{values2}{Numeric vector containing the values of the second
parameter.}

\item{f}{Function to be approximated.}

\item{grid}{Numeric vector of evaluation points.}

\item{n}{Optional positive integer giving the operator degree.
If \code{NULL}, the value stored in \code{operator} is used.}
}
\value{
A data frame of class
  \code{c("approx_parameter_scan_2d", "data.frame")} containing
  all parameter combinations, validity information, and the
  corresponding error measures.
}
\description{
Evaluates the approximation performance of a parametric
approximation operator over all combinations of two parameter
sequences.
}
\details{
For each pair of parameter values, the operator is reconstructed
and the approximation error is evaluated on the supplied grid.
The function reports the maximum absolute error, mean absolute
error (MAE), and root mean squared error (RMSE).

Invalid parameter combinations do not interrupt the complete scan.
They are retained in the result with \code{valid = FALSE},
missing error measures, and an explanatory message.
}
\examples{
\dontrun{
M <- approx_operator(
  family = "sheffer",
  subfamily = "meixner",
  n = 20,
  params = list(
    alpha = 0.5,
    beta = 2,
    theta = 1,
    c = 0.75
  )
)

parameter_scan_2d(
  M,
  parameter1 = "alpha",
  values1 = c(0.5, 1, 2),
  parameter2 = "beta",
  values2 = c(1, 2, 4),
  f = function(x) x^2,
  grid = seq(0, 1, length.out = 101)
)
}

}
