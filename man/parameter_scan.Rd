% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter-scan.R
\name{parameter_scan}
\alias{parameter_scan}
\title{Scan an Operator Parameter}
\usage{
parameter_scan(operator, parameter, values, f, grid, n = NULL)
}
\arguments{
\item{operator}{An object of class \code{"approx_operator"}.}

\item{parameter}{Character string giving the name of the parameter
to be varied.}

\item{values}{Numeric vector containing the parameter values to scan.}

\item{f}{Function to be approximated.}

\item{grid}{Numeric vector of evaluation points.}

\item{n}{Optional positive integer giving the operator degree.
If \code{NULL}, the value stored in \code{operator} is used.}
}
\value{
A data frame of class
  \code{c("approx_parameter_scan", "data.frame")} containing the
  parameter values and the corresponding error measures.
}
\description{
Evaluates the approximation performance of a parametric
approximation operator over a sequence of values of one parameter.
}
\details{
For each parameter value, the operator is reconstructed and the
approximation error is evaluated on the supplied grid. The function
reports the maximum absolute error, mean absolute error (MAE), and
root mean squared error (RMSE).
}
\examples{
\dontrun{
C <- approx_operator(
  family = "sheffer",
  subfamily = "charlier",
  n = 20,
  params = list(a = 2)
)

parameter_scan(
  C,
  parameter = "a",
  values = c(1.5, 2, 3, 5),
  f = function(x) x^2,
  grid = seq(0, 1, length.out = 101)
)
}

}
