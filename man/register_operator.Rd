% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register-operator.R
\name{register_operator}
\alias{register_operator}
\title{Register a Custom Approximation Operator}
\usage{
register_operator(
  family,
  domain,
  evaluation_engine,
  subfamily = NULL,
  variant = "discrete",
  moment_engine = NULL,
  parameter_validator = NULL,
  display_name = NULL,
  metadata = list(),
  overwrite = FALSE
)
}
\arguments{
\item{family}{Character string specifying the operator family.}

\item{domain}{Numeric vector of length two giving the operator domain.}

\item{evaluation_engine}{Function used to evaluate the operator.
It must accept the arguments \code{f}, \code{x}, and
\code{operator}, and return a numeric vector of length
\code{length(x)}.}

\item{subfamily}{Optional character string specifying the operator
subfamily. The default is \code{NULL}.}

\item{variant}{Character string specifying the operator variant.
The default is \code{"discrete"}.}

\item{moment_engine}{Optional function for computing ordinary moments.
It must accept the arguments \code{operator}, \code{order}, and
\code{x}. The default is \code{NULL}.}

\item{parameter_validator}{Optional function used to validate the
parameter list supplied through \code{approx_operator()}.
The function must accept one argument containing the parameter list.}

\item{display_name}{Optional character string used as a descriptive
name for the operator.}

\item{metadata}{Optional list containing additional operator metadata.}

\item{overwrite}{Logical. If \code{TRUE}, an existing registration
with the same family, subfamily, and variant may be replaced.}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
Registers a user-defined approximation operator so that it can be
created with \code{approx_operator()} and used with the computational
tools provided by ApproxOp.
}
\details{
A registered operator must provide an evaluation engine. A moment
engine and a parameter validator are optional. When no moment engine
is supplied, moments are computed through the general
\code{approximate()} mechanism.

Registration is atomic: if registration fails, the operator and family
registries are restored to their previous state.
}
